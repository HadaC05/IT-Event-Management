<!DOCTYPE html><html lang="en"><head><meta charset="UTF-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Choose a new password | CITE Events</title>@vite('resources/css/app.css')</head><body class="grid min-h-screen place-items-center bg-white p-5 font-sans text-[#121017]"><main class="w-full max-w-lg rounded-2xl border border-[#121017]/10 bg-white p-7 shadow-xl"><h1 class="text-2xl font-black">Choose a new password</h1><p class="mt-2 text-sm text-[#121017]/50">This reset applies only to the <strong>{{ $user->role?->name }}</strong> login <strong>{{ '@'.$user->username }}</strong>.</p><form class="mt-6 grid gap-4" method="POST" action="{{ route('password.reset.update') }}">@csrf<input type="hidden" name="user_id" value="{{ $user->id }}"><input type="hidden" name="token" value="{{ $token }}">@foreach([['password','New password'],['password_confirmation','Confirm password']] as [$name,$label])<label class="grid gap-2"><span class="text-sm font-bold">{{ $label }}</span><input class="h-12 rounded-xl border border-[#121017]/12 px-3" name="{{ $name }}" type="password" required><x-form-error :name="$name" /></label>@endforeach<button class="min-h-12 rounded-xl bg-[#397565] font-black text-white">Reset this account</button></form></main></body></html>
