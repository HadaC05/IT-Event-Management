@extends('layouts.student') @section('title','Rankings')
@section('content')<header class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-[10px] font-black uppercase tracking-[.15em] text-[#397565]">Live results</p><h1 class="mt-1 text-3xl font-black">Leaderboard</h1><p class="mt-2 text-sm text-[#121017]/50">{{ $event?->title ?? 'No active event' }} · scores are view-only</p></div></header>
@if($event)<div class="mt-6 flex gap-2 overflow-x-auto pb-2" role="tablist" aria-label="Leaderboard category"><button class="min-h-11 shrink-0 rounded-xl bg-[#397565] px-4 text-xs font-black text-white" data-rank-tab data-category="overall" aria-selected="true">Overall Ranking</button>@foreach($categories as $category)<button class="min-h-11 shrink-0 rounded-xl bg-white px-4 text-xs font-black text-[#121017]/55 ring-1 ring-[#121017]/8" data-rank-tab data-category="{{ $category->id }}" aria-selected="false">{{ $category->name }}</button>@endforeach</div><section class="mt-4 overflow-hidden rounded-3xl border border-[#121017]/8 bg-white"><div class="grid grid-cols-[55px_minmax(0,1fr)_90px_105px] border-b border-[#121017]/7 bg-[#F7F4ED] px-4 py-3 text-[9px] font-black uppercase tracking-wider text-[#121017]/40 sm:grid-cols-[70px_minmax(0,1fr)_120px_130px]"><span>Rank</span><span>Tribe</span><span class="text-right">Score</span><span class="text-right">Recent change</span></div><div data-ranking-list>@foreach($teams as $team)@php($latest=$team->scores->sortByDesc('updated_at')->first())<div class="grid min-h-16 grid-cols-[55px_minmax(0,1fr)_90px_105px] items-center border-b border-[#121017]/6 px-4 last:border-0 sm:grid-cols-[70px_minmax(0,1fr)_120px_130px]" data-rank-row data-team="{{ $team->id }}" data-overall="{{ $team->total_score }}" @foreach($categories as $category) data-score-{{ $category->id }}="{{ (float)$team->scores->firstWhere('score_category_id',$category->id)?->points }}" @endforeach><strong class="text-lg" data-rank-number>{{ $team->rank }}</strong><div class="flex min-w-0 items-center gap-3"><i class="h-3 w-3 shrink-0 rounded-full" style="background:{{ $team->color }}"></i><strong class="truncate text-sm">{{ $team->name }}</strong></div><strong class="text-right text-sm text-[#397565]" data-rank-score>{{ number_format($team->total_score,1) }}</strong><span class="text-right text-[10px] text-[#121017]/40" data-rank-change>@if($latest)+{{ number_format($latest->points,1) }} · {{ $latest->updated_at->diffForHumans(null,true) }}@else—@endif</span></div>@endforeach</div></section><p class="mt-3 text-center text-[10px] text-[#121017]/35">Category filters update instantly. Only advisers can change scores.</p>@else<div class="mt-7 rounded-3xl border border-dashed border-[#397565]/25 bg-white py-16 text-center"><h2 class="font-black">No leaderboard available</h2><p class="mt-1 text-sm text-[#121017]/45">Rankings will appear with your next event.</p></div>@endif
@endsection
